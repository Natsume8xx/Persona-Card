# P0-1J 配表契约全面同步（牌型/人格牌三表/路线奖励/全局配置/商店 4 表）

> 状态：⏸ 已暂停（2026-09-02）—— 优先处理 UI 适配任务（Qwen VL Max 接入后处理美术识别）
> 备注：计划已批准，未开工；6 步实施方案保留，待 UI 任务完成后继续
> 依据：CH 最新版 Docs/人格牌.xlsx（15 sheet）与 第二阶段开发.md 第二节「配表已更新、代码契约未同步」
> 前置：策划已拍板配表定稿，5 项全做；P0-1H 设置走查并入 P0-10 回归

---

## 一、修改计划

程序侧 5 个配表契约与最新配表漂移，按 6 步同步（每步编译零错误 + EditMode 全绿 + 独立提交）：

| # | 配表变化（最新版） | 代码现状 | 同步内容 |
|---|---|---|---|
| 1 | 牌型新增第 11 种「皇家同花顺」（HAND_11，RARE，100×12）+ 品质列 + 牌型品质定义表（NORMAL/RARE） | 判定层/枚举只认 10 种；品质定义表无人读 | Step 1 |
| 2 | 人格牌改为词条/主属性/次级属性三表 + 比较符定义表（旧「概率/品质配置」已删） | 仍是 16 行单表契约（PersonaTableMapper）；比较符表无人读 | Step 2 |
| 3 | 关卡流程新增奖励 4 列（金币 3/3/4）+ 阶段_ID/分数类型两列 + 分数上调（950~2600）+ 最终关 Boss战斗 | RunRoute.asset 仍是旧分数；RunRouteTableMapper 契约未同步 | Step 3 |
| 4 | 全局配置 13→17 条（插入商店商品槽数量 4 + 新增 4 条剩余行动兑换金币规则） | 行序/行数未同步（RequiredRuleCount=12） | Step 4 |
| 5 | 商店 4 表已填内容（商品 65 行含价格/铸造 2 行/槽位刷新 9 行/商品刷新 65 行） | 商店仍白盒：3 服务统一 2 金币 | Step 5 |

**范围界定**：本任务只做契约读取 + 资产化 + 校验；运行时逻辑接入（皇家判定生效、经济奖励发放、商店改造）分别属 P0-2~P0-7。

### Step 1 — 牌型体系：皇家同花顺 + 品质（Core → Cards → Data → UI.Editor → 测试）

1. 新增 `Core/HandQuality.cs`：`public enum HandQuality { NORMAL = 1, RARE = 2 }`（Core 无引擎引用）
2. `Core/HandType.cs`：枚举**末尾追加** `RoyalFlush = 13`（严禁重排，旧存档按 int 序列化）
3. `Core/HandTypeEntry.cs`：构造器加 `HandQuality quality` 参数 + `Quality` 属性；**删 cardId 字段**（仅 HandTypeAsset/HandTypeImportCommand 消费，无运行时读取）；`CreateFallbackList()` → 13 条（皇家 100/12/序11/RARE 插同花葫芦后；五条序 11→12、同花五条 12→13；品质按新表）
4. `Cards/Hands/HandTypeDefinition.cs`：构造器与属性加 Quality
5. `Cards/Hands/HandEvaluator.cs`：调用处算 `isRoyalFlush`（点数集合恰为 {10,11,12,13,14}，A2345 轮子不算）作第 4 参传入，StraightFlush 分支**之前**插入 `if (isRoyalFlush) return HandType.RoyalFlush;`
6. `Cards/Hands/HandTypeCatalog.cs`：零改动（fallback 驱动自动带出第 13 条）
7. `Data/HandTypeTableMapper.cs`：契约加 `ColQuality="牌型品质_ID"`、`QualitySheetName="牌型品质定义表"`，删 CardId/Image 列常量；签名 `Map(rows, ICollection<string> qualityIds)`；`TryMapHandId`：HAND_11→RoyalFlush、HAND_12→FlushFive 容错保留；**RequiredCoreTypes = HAND_01~11**（皇家已是真实表行）；品质值 ∉ 集合 → 行级错误；计分牌数警告机制保留
8. `Data/HandTypeAsset.cs`：Entry 加 quality；CreateFallbackEntries 13 条
9. `Data/ImageSheetContract.cs`：新增共享 `TryBuildBindingIds(rows)` → (列存在?, 绑定集合)——新表「绑定ID」列已删除，列缺失时调用方只发一条全局提示（否则 52 张卡逐行噪音警告）
10. `UI/Editor/HandTypeImportCommand.cs`：读两个 sheet（牌型配置 + 品质定义表提 ID 集合）→ Map(rows, qualityIds)；删 card_id 交叉校验；`CardConfigImportCommand` 改用 TryBuildBindingIds

### Step 2 — 人格牌三表 + 比较符定义表（5 新增，删旧单表链路，同步一步完成防悬空）

新增（Data 程序集，Contract+Mapper 同文件 + 独立 Asset 文件 + UI.Editor 导入命令）：

| Sheet | Mapper / Entry / Asset |
|---|---|
| 人格牌配置 | PersonaCardTableMapper / PersonaCardEntry{personaId, personaName, entryId, mainAttrId, subAttrId, maxAttrs, maxSubAttrs, subPoolSize} / PersonaCardCatalog.asset |
| 人格牌_词条 | PersonaEntryTableMapper / PersonaEntryEntry{entryId, description, conditionType, comparator, conditionParam} / PersonaEntryCatalog.asset |
| 人格牌_主属性 | PersonaMainAttrTableMapper / PersonaMainAttrEntry{attrId, attrType, param1, param2, unlockNode} / PersonaMainAttrCatalog.asset |
| 人格牌_次级属性 | PersonaSubAttrTableMapper / PersonaSubAttrEntry{subAttrId, ownerPersona, weight, attrType, param1, param2, unlockNode} / PersonaSubAttrCatalog.asset |
| 比较符定义表 | ComparatorDefinitionTableMapper / ComparatorDefinitionEntry{comparatorId, name, description} / ComparatorDefinition.asset |

- **所有参数列一律原文 string 存储**（20/8 与 0.3/0.05 混写，语义解析留给 B7）
- 词条映射器签名 `Map(rows, ICollection<string> comparatorIds)`：未知比较符 → 警告不阻塞
- 5 个导入命令走标准管线（FileShare.ReadWrite → MemoryStream → ReadTable → Map → 临时资产 → CopySerialized 覆写保 GUID），菜单「Persona Cards/导入人格牌卡牌目录数据 / 导入词条数据 / 导入主属性数据 / 导入次级属性数据 / 导入比较符定义数据」，Play Mode 置灰

删除（同步）：
- `Data/PersonaTableMapper.cs`、`UI/Editor/PersonaImportCommand.cs`、`Tests/EditMode/PersonaTableMapperTests.cs`
- `UI/Editor/BattlePrototypeSceneBuilder.cs` 的 EnsurePersonaConfigAsset：删除对 PersonaImportCommand 的引用，内联为 CreateInstance + 空 entries + CreateAsset（保持原语义）

保留不动：PersonaConfigAsset.cs、PersonaConfig.asset、InitialPersonaCatalog、PrototypeFlowController 的序列化字段——旧运行时链路持续有效直到 B7。

### Step 3 — 关卡流程：奖励 4 列 + 阶段_ID/分数类型 + 最终 Boss + 新分数

1. `Data/RunRouteTableMapper.cs`：
   - 契约新增 `ColStageId="阶段_ID"`、`ColScoreType="分数类型"`、ColRewardType1/ColRewardParam1/ColRewardType2/ColRewardParam2、ScoreTypePass="通关分数"、ScoreTypeNone="无"
   - ColShopAfter 常量保留但改注释：列已永久删除，缺列默认「是」，**取消缺列一次性警告**
   - 分数类型校验：战斗节点必须「通关分数」、生成节点必须「无」→ 违反则行级警告
   - AI节点容错：「0」按空处理（否则 10 条战斗行发噪音警告）
   - 奖励列：战斗节点读类型/参数（空→"" 原文存储；类型非「金币/无/空」→ 警告）；生成节点忽略（STAGE_12 的「0」静默容忍）
   - 最终关 BossBattle → Primary 池由既有「按出现顺序分配」逻辑自动生效，零改码
2. `Data/RunRouteAsset.cs`：RunBattleNode 追加 `stageId, rewardType1, rewardParam1, rewardType2, rewardParam2`（默认 ""）；`CreateDefaultNodes()` 白盒 13 节点同步新分数 950~2600、最终节点改 BossBattle+Primary+hasShopAfter=false、stageId 按 STAGE_01~13 填
3. RunRouteImportCommand / RunRouteAssetGenerator：零改码（数据驱动）

### Step 4 — 全局配置 12→17 条

1. `Data/GlobalConfigTableMapper.cs`：`RequiredRuleCount = 12 → 17`
2. `UI/GlobalConfig.cs` 门面加便捷属性（只暴露数据面）：`RuleShopSlots="RULE_005"`、`RuleExchangePlaysUnit/PlaysCoins/DiscardsUnit/DiscardsCoins="RULE_014~017"` + ShopSlots/ExchangePlaysUnit/... 静态属性（默认 4/1/1/1/1）
3. GlobalConfigImportCommand：零改码
4. 全仓 grep `RULE_005` 确认无旧编号消费者（已核：仅 P0-1E 的 RULE_004 读取点随 Step 2 删除）

### Step 5 — 商店 4 表资产化（P0-7 前置，纯契约读入，运行时零接线）

| Sheet | Mapper / Entry / Asset |
|---|---|
| 商品_商品 | ShopProductTableMapper / ShopProductEntry{productId, productName, productType, price, purchaseLimit, effectType, effectParam1, effectParam2}（价格/限购 int 校验≥0，效果参数原文）/ ShopProduct.asset |
| 商店_人格铸造 | ShopForgeTableMapper / ShopForgeEntry{forgeId, forgeName, price} / ShopForge.asset |
| 商店_商品槽位刷新规则 | ShopSlotRefreshTableMapper / ShopSlotRefreshEntry{refreshId, node, productType, count, weight} / ShopSlotRefresh.asset |
| 商店_商品刷新规则 | ShopPoolRefreshTableMapper / ShopPoolRefreshEntry{poolId, productId, weight} / ShopPoolRefresh.asset |

- REFRESH_004 缺号不报错（ID 按不透明字符串处理）；「人格」归一「人格牌」+ 全局警告（仿 KindLegacyNormal 别名模式）
- POLL_/POOL_ 双前缀容错；权重按真实值 1/10/20 读，校验 ≥1
- 4 个导入命令标准管线，Play Mode 置灰

### Step 6 — 文档与全量回归

- 本文件补齐开发内容/涉及文件/测试/验证记录
- `Docs/代策划确认.md`（根目录现行版）追加新疑问（见下）
- `Docs/KF/第二阶段开发.md`：P0-1J 标记完成；更正「奖励 2 列」→ 4 列、「后 8 张旧数据残留」→ 现仅 8 行
- 全量 EditMode + Regenerate Battle Prototype Scene + 各导入菜单逐个跑真实 xlsx 验证资产写入与警告符合预期

---

## 二、配表疑问（开发中写入 代策划确认.md，不阻塞导入）

1. 词条表 ID 前缀 MAIN_* 与人格牌配置引用的 ENTRY_* 断链 → 建议策划改词条表 ID 为 ENTRY_001~008
2. 商品池_ID 拼写 POLL_CARD_*（其余 POOL_*）+ REFRESH_004 跳号 → 兼容 + 请确认
3. 牌型「计分牌数」语义：HAND_03=4/顺序3、HAND_04=3/顺序4、HAND_11=4/顺序11 → 请确认该列语义
4. STAGE_12 生成节点奖励参数1=「0」（其他为「无」）→ 兼容 + 请统一
5. 商店_商品刷新规则权重 1/10/20 三档、商品池规模 52 卡/8 人格/5 服务 → 仅告知
6. 图片配置「绑定ID」列去向（现 104 行无此列，卡牌链接列全空）→ 请说明绑定方式
7. 关卡流程新增「阶段_ID/分数类型」两列——旧版无此两列，是否定稿格式
8. 人格牌配置仅 8 行（此前以为 16 行）——后 8 张何时补表

---

## 三、修改进度

| 步骤 | 状态 |
|---|---|
| Step 1 牌型体系 | ⏸ 未开始 |
| Step 2 人格牌三表 + 比较符 | ⏸ 未开始 |
| Step 3 关卡流程 | ⏸ 未开始 |
| Step 4 全局配置 17 条 | ⏸ 未开始 |
| Step 5 商店 4 表 | ⏸ 未开始 |
| Step 6 文档与回归 | ⏸ 未开始 |

---

## 四、开发内容（随步骤追加）

（待开工后逐步记录）

---

## 五、涉及文件（随步骤追加）

（待开工后逐步记录）

---

## 六、测试内容（随步骤追加）

计划新增 ~35 条、修改 6 处、删除 1 文件：

- **新增**：HandEvaluatorTests +2（皇家识别 / A2345 轮子仍 StraightFlush）；HandTypeCatalogTests +2（All 13 含皇家 / fallback 含皇家序11、五条序12、同花五条序13）；HandTypeTableMapperTests +3（11 行真实夹具含品质列 / 品质不在定义表→行错误 / 缺 HAND_11→报错）；ImageSheetContract 容错 +1；人格牌 5 mapper 测试（8/8/8/40/8 行映射、原文保留、权重/数值校验、未知比较符警告、REFRESH_004 类容错）；商店 4 mapper 测试（65/2/9/65 行、价格校验、别名归一、POLL/POOL 容错、权重 1/10/20）；GlobalConfigTests +2（ShopSlots=4、4 兑换属性默认）；RunRouteTableMapperTests +2（AI节点「0」无警告 / 分数类型与奖励列校验）
- **修改**：HandEvaluatorTests 12→13 种；HandTypeCatalogTests 计数 12→13；HandTypeTableMapperTests 夹具 10→11 行（加品质列、删 card_id）；GlobalConfigTests / GlobalConfigTableMapperTests 夹具 12→17 行；RunRouteTableMapperTests 新分数 950~2600 + 奖励 + 阶段_ID + 最终 BossBattle/Primary；RunRouteTests DefaultRouteHasExpectedStructure 新分数 + 最终 Boss + 奖励空
- **删除**：PersonaTableMapperTests（整文件）

---

## 七、验证记录（随步骤追加）

（待开工后逐步记录）
