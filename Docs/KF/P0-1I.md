# P0-1I 战斗体验对齐批

> 状态：✅ 已完成（2026-08-22）
> 依据：策划案 3.3.9（战斗信息显示）、3.3.10（手牌排序）、主界面流程图（四入口）、UI_SCREEN_SPEC UI-02/UI-04
> 对应阶段计划第 255 行「战斗体验对齐批」与 A 表第 7 项

---

## 一、拍板记录（无策划依据处由程序自定，走查时请确认）

1. **同点花色序（大小排序中「点数相同时再按花色排」的先后）**：策划案未给顺序。拍板沿用 3.3.10 花色分组的顺序：**红桃 > 方块 > 梅花 > 黑桃**。
2. **两种排序方式的切换入口**：策划案只给两种规则，未给 UI。拍板：战斗屏底部右侧（弃牌按钮旁）加一个小切换按钮「排序：大小 / 花色」，点击轮换，**只改显示**（不动牌序、选中、抽牌顺序）。排序偏好为战斗内临时状态，不写存档（读档恢复默认「大小」）。
3. **手牌默认排序**：拍板默认「大小排序」（A→2），与卡牌习惯一致。
4. **主菜单布局**：加「图鉴」「退出游戏」后共 7 个按钮，沿用用户「一行一个全宽」约束（2026-08-21 拍板）。顺序自上而下：开始游戏 / 继续游戏 / 人格收藏·装备 / 图鉴 / 设置 / 退出游戏 / 战斗教学（P0-1G 入口保留，位置沉底）。主菜单卡片加高，标题副标题压缩上移。
5. **图鉴入口**：先做占位界面（独立屏「11 Compendium Screen」：标题 + 说明「内容待策划确认」+ 返回）。策划案 B5「图鉴显示什么」仍在待确认清单（代策划确认.md），确认后替换占位。ESC 可关闭。
6. **退出游戏**：`Application.Quit()` 在编辑器 Play 模式下无效，拍板：运行时反射 `UnityEditor.EditorApplication.isPlaying = false`（UI 程序集不引用 UnityEditor，避免 asmdef 改动），真机走 `Application.Quit()`。
7. **获得金币显示**：3.3.9 要求「获得金币（本场战斗当前获得）」，但战斗内金币发放属 P0-6 奖励管线尚未实现——本任务只铺 UI 与数据接线点：`BeginBattle` 新增 `journeyCoins` 参数（当前金币，由 FlowController 注入 `_journeyDeck.Coins`），「获得金币」恒显示 `+0`，P0-6 接入后实时更新。
8. **牌库查看页签（UI-04 对齐）**：保留现有 4 个页签（全部牌 / 抽牌堆 / 已出牌 / 已弃牌），与 UI-04「当前牌库 / 已用牌 / 已弃牌」的映射：全部牌+抽牌堆=当前牌库可查看视角、已出牌=已用、已弃牌=已弃。摘要行改为 **`总数 N · 可选 M`**（可选数=该页签下还能打出的牌：全部牌/抽牌堆页签 = 手牌+抽牌堆数，已出/已弃 = 0；原「手牌/抽牌堆/已出/已弃」全景明细由页签本身承担）。**空态**：页签下无牌时摘要显示「（空）」，页码文本显示「{页签名} · （空）」。

---

## 二、开发内容

### 1. 手牌排序（纯逻辑 + 显示接线，3.3.10）

- **新增 `Assets/PersonaCards/Cards/HandDisplaySorter.cs`**（static 纯逻辑，EditMode 可测）：
  - `public enum HandSortMode { RankFirst, SuitGrouped }`（大小排序 / 花色分组）
  - `Sort(IEnumerable<PlayingCardInstance> cards, HandSortMode mode)` 返回**新数组**，不修改输入（排序只改显示顺序的根基）。
  - 大小排序：Rank 降序（Ace=14 → Two=2），同点数按花色序 红桃→方块→梅花→黑桃（拍板 1）。
  - 花色分组：红桃→方块→梅花→黑桃分组，组内 Rank 降序。
  - 花色序 helper 私有：Hearts=0 < Diamonds=1 < Clubs=2 < Spades=3（策划案分组顺序；Suit 枚举本身的 梅花<方块<红桃<黑桃 顺序不可直接用于排序，需映射）。
- **BattlePrototypeController**：
  - 新字段 `_handSortMode`（默认 RankFirst）+ `[SerializeField] private Button handSortButton;`。
  - `ConfigureScene` 追加第 27 个参数 `Button handSort`；Awake 绑定 `handSortButton.onClick.AddListener(ToggleHandSortMode)`。
  - `ToggleHandSortMode()`：轮换模式 → `RefreshHand()` + 按钮 label 更新「排序：大小」/「排序：花色」。
  - `RefreshHand()`（:247-266）改为：`HandDisplaySorter.Sort(_battle.Deck.Hand, _handSortMode)` 后按显示序 Instantiate/Configure——index 用显示位置（扇形排布随排序变化），选中状态仍按 `card.Id` 查询不受影响，`_battle.Deck.Hand` 本身不动（抽牌顺序不变）。

### 2. 战斗信息显示（3.3.9）

- **进度条**（得分/目标分完成比例）：
  - SceneBuilder `BuildRightPanel`：Score 文本上移为 (0.08, 0.43)-(0.92, 0.58)，其下新建「Score Progress Bar」：(0.08, 0.395)-(0.92, 0.425)，背景深色 Image + 子对象「Score Progress Fill」金色 Image（`Image.type = Filled`，`fillMethod = Horizontal`，anchors 全拉伸）。
  - `ConfigureScene` 追加第 28 个参数 `Image scoreProgressFill`；`RefreshAll`（scoreText 更新处 :223 同批）同步 `scoreProgressFill.fillAmount = Mathf.Clamp01((float)_battle.TotalScore / _battle.TargetScore)`。
- **金币两行**：
  - resourceText 改为 5 行：`剩余出牌：N / M\n剩余弃牌：N / M\n牌堆：N\n获得金币：+0\n当前金币：N`；文本区扩为 (0.25, 0.215)-(0.90, 0.395)；图标改为 5 个（♠ / × / ▣ / ◉ 获得 / ◉ 当前），y 分别为 0.35/0.319/0.288/0.257/0.226。
  - `BeginBattle` 追加可选参数 `int journeyCoins = 0` 存 `_journeyCoins` 字段；`RestoreBattle` 同步加参数（读档恢复金币显示，来源 FlowController 存档数据）；resourceText 刷新时用 `_journeyCoins` 与 `_battleCoinsGained`（预留字段恒 0，P0-6 接入）。
- **FlowController**：`StartCurrentBattle` 的 BeginBattle 调用追加 `journeyCoins: _journeyDeck.Coins`；`ContinueSavedRun`/`RestoreBattle` 路径同步传 `_journeyDeck.Coins`。

### 3. 主菜单四入口（人格牌 / 图鉴 / 设置 / 退出游戏）

- **SceneBuilder `BuildPrototypeFlow`**：
  - mainCard 加高：(0.30, 0.06)-(0.70, 0.94)；Title 74→68 号上移 (0.08, 0.84)-(0.92, 0.95)；Subtitle 26→24 号 (0.08, 0.78)-(0.92, 0.84)。
  - 7 按钮一行一个全宽（x 均 0.17-0.83，y 从下往上，每行高 0.09 + 0.014 间隔）：战斗教学 0.02-0.11 / 退出游戏 0.124-0.214 / 设置 0.228-0.318 / 图鉴 0.332-0.422 / 人格收藏·装备 0.436-0.526 / 继续游戏 0.54-0.63 / 开始游戏 0.644-0.734。
  - 新建屏「11 Compendium Screen」：CreateScreenRoot + CreateBackground + 卡片 (0.30,0.12)-(0.70,0.88) + 标题「图鉴」48 号 + 占位说明「牌型规则 / 基础计分 / 人格图鉴 —— 内容待策划确认（B5）」+ 「← 返回」按钮；默认 `SetActive(false)`。
  - `FlowReferences` 加 compendiumScreen / compendiumButton / compendiumBackButton / quitButton；校验列表加 "11 Compendium Screen" / "Compendium Button" / "Compendium Back Button" / "Quit Button" / "Hand Sort Button" / "Score Progress Bar" / "Score Progress Fill"；serializedFlow 属性名列表同步加对应字段。
- **PrototypeFlowController**：
  - `Configure` 追加参数：`GameObject compendiumScreen, Button openCompendium, Button compendiumBack, Button quitGame`。
  - Awake 绑定：`openCompendium.onClick.AddListener(OpenCompendium)`、`compendiumBack.onClick.AddListener(CloseCompendium)`、`quitGame.onClick.AddListener(QuitApplication)`。
  - `OpenCompendium()`：`compendiumScreen.SetActive(true)`（主菜单层屏，Render 控制与主菜单互斥——并入现有 Render 主菜单分支）。
  - `CloseCompendium()`：`compendiumScreen.SetActive(false)`。
  - ESC 处理：主菜单态下 ESC 关图鉴（若有开）→ 开设置（P0-1H 现有逻辑），即图鉴打开时 ESC 优先关图鉴。
  - `QuitApplication()`：反射 `UnityEditor.EditorApplication.isPlaying = false`，反射失败（真机）走 `Application.Quit()`；注释说明缘由（拍板 6）。
- 图鉴屏与设置屏同属主菜单层：`Render` 的主菜单分支需在图鉴打开时保持主菜单屏可见（图鉴是覆盖屏），退出图鉴回主菜单。

### 4. 牌库查看页签对齐（UI-04）

- **BattlePrototypeController.RefreshDeckViewer**：
  - summary 改 `deckViewerSummaryText.text`：该页签空 → `（空）`；否则 `总数 {zoneCount} · 可选 {available}`（available = 全部牌/抽牌堆页签时 `DrawPile.Count + Hand.Count`，已出/已弃页签为 0）。
  - 页签空时 `deckViewerPageText.text = $"{ZoneName(zone)} · （空）"`。
  - 现有空卡片槽 SetActive(false) 处理保留。

---

## 三、涉及文件

**新增**：
- `Assets/PersonaCards/Cards/HandDisplaySorter.cs`
- `Assets/PersonaCards/Tests/EditMode/HandDisplaySorterTests.cs`
- `Docs/KF/P0-1I.md`

**修改**：
- `Assets/PersonaCards/UI/BattlePrototypeController.cs`（排序/进度条/金币/牌库摘要）
- `Assets/PersonaCards/UI/PrototypeFlowController.cs`（主菜单两按钮/图鉴屏/退出游戏/金币注入）
- `Assets/PersonaCards/UI/Editor/BattlePrototypeSceneBuilder.cs`（主菜单重排/图鉴屏/进度条/资源区扩行/排序按钮/校验）

**不动**：asmdef 全部；JourneyDeckState（金币数据已有）；存档 schemaVersion；BattleStateMachine（战斗内金币发放属 P0-6）。

---

## 四、测试内容（EditMode，新增 HandDisplaySorterTests 6 条 + 全量回归）

1. 大小排序：混合点数手牌 → A→2 严格降序。
2. 同点花色序：四张同点数 → 红桃→方块→梅花→黑桃。
3. 花色分组：混合牌 → 四组组序 红桃→方块→梅花→黑桃，组内点数降序。
4. 输入不可变：Sort 前后原 IEnumerable 序列不变、返回为不同实例。
5. 边界：空序列 / 单张不抛异常、结果与输入一致。
6. 双模式同一元素集：两种模式结果是同一多集（顺序不同、元素相同），且 8 张满手牌两组结果均含全部 8 张。

---

## 五、人工走查（Play 模式）

1. 主菜单：7 个按钮一行一个（开始/继续/收藏/图鉴/设置/退出/战斗教学），无重叠；标题排版正常。
2. 点「图鉴」→ 占位屏弹出；「← 返回」与 ESC 均可关闭；图鉴打开时 ESC 不弹设置。
3. 点「退出游戏」→ 编辑器停止 Play（真机为退出应用）。
4. 进战斗：底部「排序：大小/花色」按钮点击轮换，手牌显示顺序切换；选中的牌选中态不丢失；抽牌/出牌后牌序仍是所选模式。
5. 得分进度条：开局 0；出牌得分后 fillAmount 增长（接近目标分时接近满）。
6. 右栏资源区 5 行：「获得金币：+0」「当前金币：N」与主菜单商店金币一致（新局 3）。
7. 牌库查看：摘要「总数 N · 可选 M」；切到空页签（如战斗刚开始的「已出牌」）显示「（空）」与「已出牌 · （空）」。
8. 存档读档：中途退出 → 继续游戏 → 金币显示与存档一致；手牌排序恢复默认「大小」。

---

## 六、验证记录

**2026-08-22（程序自测）**

1. **编译**：`refresh_unity` 全绿，Console 零错误零警告（除中间态场景校验噪音，见下）。
2. **EditMode 测试**：全量 **210/210 通过**（204 旧 + 6 新 HandDisplaySorterTests），连续两轮全绿。
3. **场景重建**：执行 `Regenerate Battle Prototype Scene` 成功；场景对象清单、serialized 字段绑定、主菜单布局坐标验证全部通过。编译期 domain reload 的 delayCall 会对旧场景报一次「missing 11 Compendium Screen」校验噪音（与 P0-1H「missing 05 Settings Screen」同源），重建后消失，非问题。
4. **Play 自动化走查**（execute_code 驱动）：
   - ✅ 主菜单：7 按钮全部激活且顺序正确（开始/继续/收藏/图鉴/设置/退出/战斗教学）；图鉴屏默认关闭。
   - ✅ 图鉴：点击打开占位屏（标题「图鉴」+ 占位说明 + 返回按钮）→ 返回关闭 → 再开再关正常。
   - ✅ 新局流程：开始 → 人格装备确认 → 直接进战斗屏（首战为普通战斗）。
   - ✅ 排序按钮：文案「排序：大小」初始正确；点击后手牌显示顺序切换（大小排序 A♦Q♣J♦J♠10♦9♦7♠2♠ 降序 ✓；花色分组 ♦♦♦♦→♣→♠♠♠ 组序 ✓）。
   - ✅ 资源区 5 行：`剩余出牌/剩余弃牌/牌堆/获得金币：+0/当前金币：3`，金币 3 与 JourneyDeckState 同步。
   - ✅ 进度条：初始 fillAmount=0、type=Filled、fillMethod=Horizontal；出牌后 0→440 分，fillAmount 0→0.8（=440/550 精确同步）。
   - ✅ 牌库查看：全部牌「总数 52 · 可选 52」+ 第 1/3 页；抽牌堆「总数 44 · 可选 52」（可选=抽牌堆+手牌，语义正确）；已出牌/已弃牌空态「（空）」+「已出牌 · （空）」（开局未出牌 ✓）；关闭恢复正常。
   - ⚠️ 走查环境说明：编辑器失焦时 macOS 冻结编辑器更新循环（App Nap），frameCount 停 1-2，Destroy 帧末删除不执行，导致手牌 Instantiate 累积（8→16→32）——**环境伪象非代码问题**（正常盯着编辑器走查时无此现象，且排序显示顺序/选中逻辑验证正确）。
5. **留人工走查**（自动化无法覆盖）：ESC 关图鉴（依赖帧输入）、退出游戏按钮（会停 Play）、出牌后的隔帧清理、存档读档金币恢复。

**待用户人工走查（§五 8 步）确认后完结。**

**2026-08-22 用户走查反馈修复（两轮）**

用户 Play 走查发现 4 个 UI 问题，逐一修复：

1. **得分进度条一直是满的**（根因：`Score Progress Fill` 的 Image 无 sprite——Unity 对无 sprite 的 Image 不裁剪，Filled 永远画满整条；fillAmount 属性其实一直在正确更新）。修复：先挂内置纯白 sprite（`UI/Skin/Background.psd`）。
2. **进度条从中间开始填充**（第二轮反馈）：内置 sprite 带 4px border，Filled + 9-slice 边界导致填充从中间起步。**最终方案：弃用 Filled，改 Simple + anchorMax.x 驱动宽度**——`RefreshAll` 里 `anchorMax.x = Lerp(左缘, 右缘, ratio)`，左右缘在 Awake 时从场景 rect 读取（锚点改布局无需改代码），确定从左向右增长。
3. **资源区几行没对齐**（根因：5 行文本顶部紧凑排列、图标均匀铺满整个区域，第 5 行错位约 93px）。修复：图标 y 逐行对齐文本行中心（0.3757/0.3572/0.3387/0.3201/0.3016，按 20 号字行高 20px/1080 计算），图标徽章缩到贴近行高（高 0.04、宽 0.085、字号 20），文本区顶 0.395→0.385 避免徽章压进度条。
4. **排序按钮减半**：(0.20×0.10)→(0.10×0.05)，与出牌/弃牌按钮垂直居中，字号 28→15。
5. **牌库查看/牌型规则按钮缩小**：高 0.09→0.075、宽 0.39→0.30，水平居中，字号 28→24。

按钮大小用户已确认 OK；进度条填充方向修复后待用户最终确认。

**2026-08-22 用户最终确认 + 计分疑问排查**

1. 进度条从左向右填充，用户确认没问题，本任务完结。
2. 用户走查中提问：单张 7 打出「筹码 77 × 倍率 5.5 ≈ 400 分」是否太高，是配表数据如此还是代码出错。排查结论（详见 代策划确认.md B9）：
   - 实时取证 HUD：`当前牌型：高牌　筹码 77 × 倍率 5.5　预计得分：424`。
   - 77 = 55（高牌基础配表）+ 7（牌面，卡牌配置正确）+ 15（教学卡「积累者」高牌 +15 筹码）。
   - 5.5 = 1（基础）+ 2.5（铸牌卡「偏转·调律者」）+ 2（教学卡「执行者」高牌 +2 倍率）。
   - 424 = 77 × 5.5，计分管线计算无误。**非代码 bug、非配表错误**：数值全部可解释，属 P0-1E 白盒自定数值（教学卡 + 铸牌池）叠加导致的平衡问题，已按惯例记入 代策划确认.md 待策划拍板。
