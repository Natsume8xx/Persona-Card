# 《人格牌》Unity 工作约定

- 最新策划确认（2026-09-08）：一切以 `E:/GameDemo/MVP` 当前网页内容为最高准则。当前为 17 核心节点；旧 13 节点和三战文档属于历史材料。
- 开工先读 `Docs/Migration/WEB_BASELINE.md`。原生迁移入口为 `Assets/Scenes/PersonaWebAligned.unity`。
- 网页导出是只读生成物。通过 `Tools/WebMigration/export-core.cjs` 更新，禁止在导出或 C# 中另外维护相同策划数值。
- 战斗、随机、人格生成与胜负必须由本地规则验证；不得接收模型直接提供的可执行规则或最终胜负。客户端禁止生产 API Key。
- 保留原场景、原资源及 228 项旧测试。不重新启用旧自动场景重建器覆盖手工场景。
- 长期存档变动需要明确版本策略，恢复不得重抽已经生成的内容。更新后执行旧回归、新迁移测试及原生画面验收。
- 当前验证平台 Windows x64。其他平台须单独安排原生脚本内核与打包验证。

- 最新交付要求（2026-09-09）：每次完成后，必须向用户说明本次完成、仍未开发或未补齐、尚未验证的内容，并同步维护 Docs/Migration/REMAINING_WORK.md；不得用笼统完成百分比代替缺口清单。


## 上下文压缩后的接续要求（2026-09-09 最新用户要求）

- 每次继续前读取 Docs/Migration/HANDOFF.md、FEATURES_IMPLEMENTED.md、REMAINING_WORK.md、final-verification.json，并检查实际工程变化。
- 每次交付写明本轮新增、累计已实现、未实现和未验收内容；同步维护以上文件。历史聊天不能代替工程与验证证据。
- 不把已有功能等同于视觉已完全对齐；不把自动胜利夹具等同于正常人工通关。
